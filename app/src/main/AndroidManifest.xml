<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.abhyuday.treeplantation">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserProfile"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PlantByOrg.SingleTreeModule.SingleTreeData" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".PlantByOrg.TreeListModule.PlantList"
            android:windowSoftInputMode="stateHidden" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".PlantByOrg.PlantingTreeModule.PlantHereMap"
            android:label="@string/title_activity_plant_here_map" />
        <activity android:name=".PlantByOrg.PlantingTreeModule.TreePlantedMessage" />
        <activity android:name=".PlantByOrg.MyTreesModule.MyTreeList" />
        <activity
            android:name=".PlantByOrg.MyTreesModule.SpotAllYourTrees"
            android:label="@string/title_activity_spot_all_your_trees" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".PlantByOrg.MySingleTreeModule.MyTreePhotos" />
        <activity android:name=".PlantByOrg.MySingleTreeModule.MySingleTree" />
        <activity android:name=".PlantByOrg.MySingleTreeModule.UploadMyTreePhoto" />
        <activity android:name=".PlantByYourself.PlantHere.PlantHereActivity" />
        <activity android:name=".PlantByYourself.UploadNearestModule.UploadNearestTree" />
        <activity android:name=".PlantByYourself.GalleryModule.MyGallery" />
        <activity android:name=".PlantByYourself.GalleryModule.MyFolder" />
        <activity
            android:name=".PlantByYourself.SpotAllOnMapModule.SpotYourTrees"
            android:label="@string/title_activity_spot_your_trees"></activity>
    </application>

</manifest>